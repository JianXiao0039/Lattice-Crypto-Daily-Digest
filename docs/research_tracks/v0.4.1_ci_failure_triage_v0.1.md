# v0.4.1 CI Failure Triage v0.1

## Classification

`environment_difference_unresolved`

Evidence:

- exact `HEAD` archive passes 521 repository tests locally under Python 3.15.0b2;
- GitHub CI uses Python 3.11;
- Ubuntu and Windows both fail at `Run tests`;
- the failure is therefore not demonstrated to be Windows path-specific;
- no evidence attributes it to shadow-mode, but CI logs are unavailable.

Do not weaken tests or change production behavior without the failed test output. Next action is authenticated log inspection or a controlled Python 3.11 reproduction.
