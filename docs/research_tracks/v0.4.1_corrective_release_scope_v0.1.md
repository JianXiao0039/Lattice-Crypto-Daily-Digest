# v0.4.1 Corrective Release Scope v0.1

## Included

- active version alignment to `0.4.1`;
- portable repository-relative reliability paths;
- deterministic strict doctor health independent of unrelated staging state;
- real critical `Asia/Singapore` validation;
- focused regression tests;
- historical `v0.4.0` inconsistency documentation;
- first post-tag run durability review.

## Excluded

- ranking, taxonomy, queries, negative keywords, classifier, fetcher, and relevance semantics;
- source retry behavior;
- private workspace content;
- scheduler or background service changes;
- moving or recreating `v0.4.0`;
- creating `v0.4.1` tag in this phase.
