# Automation Module Prompt Recommendations v0.2

Status: public prompt recommendation doc for manual UI updates.

# Daily Public Digest Run

Use:

- `docs/research_tracks/daily_public_digest_run_prompt_v0.2.md`

Key changes from v0.1:

- add reliability dashboard step;
- require explicit source-starved interpretation;
- require IACR latest and Semantic Scholar status visibility;
- require `git status -sb` in the final validation summary;
- use only working repository commands.

# Weekly Public Synthesis Run

Use:

- `docs/research_tracks/weekly_public_synthesis_run_prompt_v0.2.md`

Key changes from v0.1:

- reference actual weekly handoff commands only;
- mention missing-day / partial-week coverage explicitly;
- require empty handoff explanation.

# Full Manual Quality Run

Use:

- `docs/research_tracks/full_manual_quality_run_prompt_v0.2.md`

Key changes from v0.1:

- keep paused;
- position it as a heavy manual validation/recovery profile only;
- explicitly forbid using it as default daily automation.
