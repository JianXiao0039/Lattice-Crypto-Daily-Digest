# v0.4.1 Remaining Blockers v0.1

1. Publish the narrow date-specific Markdown/JSON force-add contract through a separately approved Git change.
2. Obtain green Ubuntu and Windows CI on the resulting commit.
3. Observe a Daily run that retains both artifacts and verifies them in `origin/main`.
4. Preserve source-health and validation evidence for that run.
5. Reconcile the already-published tag with the recovery evidence without modifying tag history.

The local package version and release hygiene are not the current blockers.
