# v0.5 Monthly Synthesis Implementation v0.1

Status: `monthly_synthesis_implemented`.

Command:

```powershell
python -m lattice_digest.monthly_synthesis --month YYYY-MM
```

Default outputs:

- `data/monthly/YYYY-MM.json`
- `digests/monthly/YYYY-MM.md`

Implementation file:

- `src/lattice_digest/monthly_synthesis.py`

Scope:

- Manual-only module entrypoint.
- Reads existing daily JSON.
- Lists overlapping weekly JSON when available, but daily JSON remains the primary record source.
- Does not fetch sources.
- Does not alter ranking, score, taxonomy, query expansion, source health, Daily workflow, or Weekly workflow.

Phase 13K generated:

- `data/monthly/2026-06.json`
- `digests/monthly/2026-06.md`

Workflow integration:

Deferred. The standalone module entrypoint is lower risk and preserves existing Daily/Weekly workflow semantics.
