# v0.4.1 Ubuntu CI Status v0.1

- Run: `27486931415`
- Job: `Test (ubuntu-latest)`
- Result: failure
- Failed step: `Run tests`
- Exact failure: `TODO_VERIFY` because authenticated logs are unavailable

The current CI failure affects both supported matrix platforms.
