# v0.4.1 CI Confirmation v0.1

Decision: `ci_red_both_platforms`.

Latest CI run `27486931415` targets current `origin/main` commit `57a7b3a`. It completed with failure on June 14, 2026. Both matrix jobs failed at `Run tests`.

The public GitHub Actions API supplied run and job metadata. `gh` is installed but unauthenticated, so failed log text and exact test names remain unavailable.
