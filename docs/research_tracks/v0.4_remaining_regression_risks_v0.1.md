# v0.4 Remaining Regression Risks v0.1

## Blocking Risk

### Ubuntu CI test failure

- Windows CI succeeds.
- Ubuntu CI exits during `Run tests`.
- Public annotations do not identify the test.
- Status: blocking until logs are obtained or the failure is reproduced.

## Publication Risks

### Ignored future daily artifacts

`digests/*.md` and `data/*.json` match ignore rules. Existing tracked Markdown updates work, but a new date can remain local unless the explicit publication flow handles ignored files.

### Local-only structured data

June 4-10 JSON files exist locally but are not in `origin/main`.

## Reliability Risks

### Source-starved marker incomplete

All-red empty artifacts are preserved, but `source_starved` is not explicit in current output metadata or Markdown.

### Weekly input stale

W23 still records June 6 and June 7 as missing. Handoff output is therefore structurally valid but based on incomplete weekly coverage.

### Semantic Scholar degradation

Recent usable days show Semantic Scholar yellow with no candidates. This is an enrichment limitation, not evidence of irrelevance.

## Required Regression Checks

- Linux/Python 3.11 project tests.
- Date-targeted and source-starved focused tests.
- New-date Git tracking audit.
- Weekly loaded-day coverage audit.
- Release hygiene and `git diff --check`.

