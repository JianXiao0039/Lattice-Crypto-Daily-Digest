# v0.5 Monthly Real Case Fix List v0.1

## Decision

Monthly rationale quality passed with limits.

## Concrete Fix List

1. Align rendered reading action with monthly bucket.

   Current issue: several `Should Skim`, `Track Later`, and `Ignore / Peripheral` entries still render rationale text beginning with `精读`.

   Example affected cases:

   - `BRaccoon: Concurrently Secure Blind Lattice Signatures from Raccoon`
   - `Rank Ceiling for Twiddle-Perturbation Faults on the Forward NTT`
   - `Bootstrapping is All You Need: Secure Transformer Inference via Improved CKKS Functional Bootstrapping`
   - `Butterfly Effect: Multi-Key FHE from Ring-LWR`
   - `Achieving Shannon Capacity for Computationally Bounded Errors`

2. Add rendered bilingual block for top Monthly papers or explicitly mark the compact Chinese-only rendering as intentional in the Monthly output.

   Current state: bilingual policy is documented but the real 2026-06 Monthly Markdown does not render an English counterpart block for top papers.

3. Keep source-starved caveats visible near Monthly conclusions.

   Current state: source-starved days are present and explicit. Future rendering should keep them visible near any quiet-month interpretation.

## Not a Fix in This Phase

Do not change ranking scores, score thresholds, taxonomy semantics, source fetchers, query expansion, negative keywords, or paper inclusion.
