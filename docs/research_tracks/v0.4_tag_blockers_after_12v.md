# v0.4 Tag Blockers After Phase 12V

## Blocking Conditions

1. GitHub Actions is red for tag target `08c5f07`.
2. The Windows job fails because the test update was committed without the matching path-normalization implementation.
3. Phase 12U decision remains `tag_blocked_by_ci`.
4. Package, CHANGELOG, and release documentation remain at `0.3.3`.
5. A remote lightweight `v0.4.0` tag already exists and cannot be safely replaced without an explicit history policy.

## Required Recovery

1. Prepare a focused commit containing `src/lattice_digest/reliability_dashboard.py` and the intended release metadata.
2. Exclude unrelated `.gitignore`, `papers.db`, `.arts/`, and research artifact files.
3. Push the release candidate commit.
4. Confirm green Ubuntu and Windows CI.
5. Decide explicitly between preserving `v0.4.0` and releasing a corrected `v0.4.1`, or deleting and recreating `v0.4.0` with acknowledged tag-rewrite risk.
6. Create an annotated tag only after that decision and final validation.
