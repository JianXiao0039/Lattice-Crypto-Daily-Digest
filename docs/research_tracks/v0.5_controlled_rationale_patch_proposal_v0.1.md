# v0.5 Controlled Rationale Patch Proposal v0.1

Status: proposal ready.

Patch summary:

- Add `src/lattice_digest/recommendation_rationale.py`.
- Add focused tests for evidence-basis behavior, title-only behavior, weak FHE application handling, and Python 3.11 compatibility.
- Do not integrate the helper into Daily or Weekly production rendering yet.

Future controlled patch:

1. Add rationale fields to JSON output behind a compatibility review.
2. Render concise rationale text in Daily Markdown for A/B papers.
3. Reuse Daily rationale in Weekly synthesis.
4. Keep monthly synthesis as a separate manual design unless implemented later.

Risk posture:

The helper improves explanation quality without changing ranking, taxonomy, source selection, source health, or workflow execution.
