# Post-v0.4 Known Risks v0.1
DBLP TLS and Semantic Scholar enrichment issues may recur. Source failure must remain advisory and visible rather than silently changing relevance conclusions.
## CI Portability

Ubuntu CI test failure remains unresolved. Local Windows success is not sufficient evidence of cross-platform release readiness.

## Generated Artifact Publication

New daily Markdown and JSON paths match ignore rules. Generation and publication are separate states and must be monitored independently.

## Structured Data Gap

June 4-10 JSON exists locally but is absent from `origin/main`.

## Source-Starved Visibility

All-red empty artifacts retain source health, but source-starved is inferred rather than explicitly serialized.

## Weekly Staleness

Current W23 coverage predates the complete local backfill. Handoff results inherit this limitation.

## External Source Degradation

DBLP TLS and Semantic Scholar enrichment issues may recur. Source failure must remain advisory and visible rather than silently changing relevance conclusions.
