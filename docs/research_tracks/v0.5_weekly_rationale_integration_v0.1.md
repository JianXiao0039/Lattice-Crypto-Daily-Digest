# v0.5 Weekly Rationale Integration v0.1

Status: `weekly_rationale_integrated_with_limits`.

Integration point:

- `src/lattice_digest/weekly_synthesis.py`
- Weekly Markdown now calls `build_recommendation_rationale(record)` inside `_record_line`.

Rendering policy:

- Weekly output uses a compact paragraph rather than the full Daily block.
- Top A-level papers and section records include:
  - concise paper-work summary,
  - radar relevance,
  - recommended action,
  - evidence basis,
  - TODO_VERIFY caveat.

Scope:

- Markdown explanation only.
- Weekly aggregation and deduplication are unchanged.
- Weekly JSON payload shape is unchanged.
- Weekly ordering is unchanged.
- Source-starved and source-health interpretation are unchanged.
