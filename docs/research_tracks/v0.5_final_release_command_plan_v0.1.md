# v0.5 Final Release Command Plan v0.1

## Decision

`tag_plan_blocked`

This file intentionally does not present tag commands as ready to execute.

## Required Pre-Tag Gates

Before a future tag phase may execute release commands, all of the following must be true:

1. `git status -sb` shows only the intended release state.
2. Python 3.11 dynamic import compatibility fix is committed and pushed.
3. GitHub Actions is green on Ubuntu and Windows.
4. Full local tests pass.
5. Release hygiene passes.
6. Durable Daily/Weekly/Monthly artifact verification passes.
7. Source-health degradation is explicit and not misrepresented as full recovery.
8. Phase 13R monthly real-case rationale quality remains acceptable.
9. Release notes are clean and accurate.
10. v0.4.1 remains documented as a historical blocked corrective tag.

## Current Command Status

Tag commands are blocked.

The future tag name recommendation is:

`v0.5.0`

## Later-Phase Commands Only

No tag command is prepared as ready in Phase 13S.

If and only if all gates pass in a later user-confirmed release phase, prepare a fresh release command draft at that time. That later draft should include a status check, an annotated tag creation command for the chosen semver tag, and a tag push command. Those commands are intentionally not written here as copyable commands because the current plan is blocked.

No release command was executed in Phase 13S.

## Non-Goals

- Do not create or move tags in this phase.
- Do not push release tags without explicit user confirmation.
- Do not treat local tests as remote CI green.
- Do not use v0.5 release tagging to modify ranking, source fetching, taxonomy, query expansion, or Daily/Weekly/Monthly workflow behavior.
