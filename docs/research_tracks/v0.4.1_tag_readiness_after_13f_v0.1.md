# v0.4.1 Tag Readiness After 13F v0.1

Decision: `blocked_by_multiple_conditions`.

An annotated `v0.4.1` tag already exists, so this document records recovery readiness rather than authorizing another tag. Current blockers are both-platform CI failure, missing durable Daily evidence, and an unpublished generated-artifact allowlist correction.

Do not move, recreate, force-update, or push a replacement tag from this phase.
