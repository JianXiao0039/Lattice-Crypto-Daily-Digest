# v0.5 DBLP TLS Recovery Policy v0.1

Status: `compliant_manual_retry_policy_ready`.

DBLP TLS failures must be separated from DNS, timeout, HTTP status, and parser failures.

## Required Behavior

- Keep SSL certificate verification enabled by default.
- Classify certificate/handshake issues as `ssl_failure`.
- Treat local CA/proxy remediation as an environment issue, not a source-ranking issue.
- Prefer official DBLP API endpoints already used by the project.

## Phase 13L Result

The low-load probe reached the DBLP API with HTTP 200 and one parsed hit. No TLS failure occurred in this local observation.

## TODO_VERIFY

- Re-check on GitHub Actions if DBLP fails only in CI.
- Document local CA/proxy state if SSL errors recur.
