# v0.5 Monthly Title-Only and Metadata-Only Policy Review v0.1

## Decision

`policy_present_no_selected_title_only_case`

## Evidence

Monthly payload `data/monthly/2026-06.json` reports:

- `TODO_VERIFY.papers_lacking_abstract`: one paper.
- `TODO_VERIFY.title_only_or_metadata_supported_rationales`: none.

The eight sampled cases all had `abstract_supported` evidence status.

## Interpretation

The real monthly sample did not include a title-only rationale case. The policy still exists and the audit tests verify that title-only fixtures without `TODO_VERIFY` are flagged.

## Required Behavior

Title-only or metadata-only records must:

- keep low-confidence caveats;
- avoid invented method/contribution;
- include `TODO_VERIFY`;
- avoid `精读` unless other evidence strongly supports the action.
