# v0.4 Release Tag Decision v0.1

## Decision

`tag_blocked_by_ci`

## Reason

The Linux failure is locally fixed and the Linux/Python 3.11 suite passes, but GitHub Actions has not validated the fix. The repository also still declares version `0.3.3` and lacks v0.4 release metadata.

## Transition to tag_ready_after_ci_green

Requires:

1. push the CI fix commit;
2. obtain green Ubuntu and Windows jobs;
3. prepare consistent v0.4 version metadata, CHANGELOG, and release document;
4. rerun release hygiene.

## Tag Creation

No tag is created in Phase 12U. User approval is required after final validation. Tag push requires separate approval.

