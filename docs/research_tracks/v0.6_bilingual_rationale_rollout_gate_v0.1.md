# v0.6 Bilingual Rationale Rollout Gate v0.1

Status: `eligible_for_v0_6_bilingual_quality`.

Gate checks:

- bilingual helper exists and is deterministic;
- terminology table exists;
- top Monthly rendering includes Chinese and English blocks;
- top Weekly rendering includes Chinese and English blocks;
- title-only fallback is conservative;
- missing conclusion does not produce conclusion-derived wording;
- weak peripheral records are not overpromoted;
- no manual annotation fields are required;
- no external LLM or network paper fetching is used;
- tests pass;
- release hygiene passes.

Known limit:

- Monthly reading-action text can still be stronger than the rendered monthly bucket for some lower-priority cases. This is a pre-existing alignment issue outside Phase 14B.
