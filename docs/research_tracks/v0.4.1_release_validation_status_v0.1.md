# v0.4.1 Release Validation Status v0.1

## Local Status

- active versions: `0.4.1` across all three sources;
- `v0.4.0` target unchanged;
- portable paths: fixed locally;
- strict doctor: fixed locally;
- narrow Daily submission allowlist: implemented locally;
- staging gate: clean at Phase 13A start;
- release hygiene: passes locally.
- repository tests: 471 passed locally;
- candidate verifier: all local checks pass, while remote/durable gates remain open.

## External Status

- origin/main does not contain the complete working-tree hotfix;
- latest Ubuntu and Windows CI are red;
- no durable post-tag Daily run exists;
- narrow allowlist has not run in GitHub Actions.

Overall: corrective release validation is locally promising but externally incomplete.
