# v0.4.1 Windows CI Status v0.1

- Run: `27486931415`
- Job: `Test (windows-latest)`
- Result: failure
- Failed step: `Run tests`
- Exact failure: `TODO_VERIFY` because authenticated logs are unavailable

This is not a Windows-only failure because Ubuntu failed at the same step.
