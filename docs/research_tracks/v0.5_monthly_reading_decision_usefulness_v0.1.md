# v0.5 Monthly Reading Decision Usefulness v0.1

## Decision

`reading_decision_useful_with_limits`

## Useful Evidence

The sampled Monthly cases provide:

- title;
- bucket or top-paper status;
- score;
- evidence status;
- problem/method/contribution fields;
- radar relevance;
- `TODO_VERIFY`;
- source-health caveats at month level.

## Limits

The rationale text sometimes conflicts with the rendered Monthly bucket:

- `Should Skim` entries may still say `精读`.
- `Track Later` entries may say `扫读` or `精读`.
- `Ignore / Peripheral` sample may say `精读`.

This makes the paper-work summary useful but the final action wording less reliable.

## Recommended Follow-Up

Keep ranking unchanged, but align explanation-layer action text with Monthly reading buckets.
