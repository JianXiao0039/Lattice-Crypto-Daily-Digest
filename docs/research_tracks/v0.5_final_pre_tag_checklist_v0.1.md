# v0.5 Final Pre-Tag Checklist v0.1

## Decision

`tag_plan_blocked`

## Checklist

| Item | Required | Current Status |
| --- | --- | --- |
| Python 3.11 dynamic import fix | yes | locally fixed and focused tests pass |
| Commit containing fix | yes | not created in this phase |
| Remote CI green | yes | pending after future push |
| Local full tests | yes | required in final release phase |
| Release hygiene | yes | required in final release phase |
| Durable artifacts verified | yes | previously verified, should be rechecked |
| Source health explicit | yes | previously acceptable with explicit degradation |
| Monthly rationale quality | yes | passed with limits in Phase 13R |
| Release notes clean | yes | previously blocked |
| Operations runbook | yes | exists |
| No manual annotation dependency | yes | satisfied |
| No background automation | yes | satisfied |

## Required Recheck Commands

```powershell
python --version
python -m lattice_digest.workflow doctor
python -c "import lattice_digest; print(lattice_digest.__version__)"
python scripts\verify_v0_5_rc.py --date 2026-06-15 --week 2026-W25 --month 2026-06
python scripts\verify_durable_artifacts.py --date 2026-06-15 --week 2026-W25 --month 2026-06
scripts\run_project_tests.bat
python scripts\check_release_hygiene.py
git diff --check
git diff --cached --check
git status -sb
```

GitHub Actions must also be verified green after the fix commit reaches the remote.
