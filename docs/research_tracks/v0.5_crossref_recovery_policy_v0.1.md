# v0.5 Crossref Recovery Policy v0.1

Status: `source_health_diagnostics_ready`.

Crossref failures must distinguish query mismatch, empty valid response, HTTP failure, and network failure.

## Required Behavior

- Use the official Crossref REST endpoint.
- Treat zero-item valid responses as `empty_response`.
- Respect polite usage conventions where configured.
- Do not hard-fail the radar solely because Crossref is degraded.

## Phase 13L Result

The low-load probe reached Crossref with HTTP 200 and one parsed item.

## TODO_VERIFY

- Confirm future empty responses against query/date filters.
