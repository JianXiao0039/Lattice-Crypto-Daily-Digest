# v0.5 Shadow-Mode Runbook v0.1

## Manual Command

```bat
python scripts\run_v0_5_shadow_mode_pilot.py
```

Optional explicit output:

```bat
python scripts\run_v0_5_shadow_mode_pilot.py --output-dir audits\shadow\v0_5_controlled_pilot --run-id manual-review-001
```

## Inspect

Review `manifest.json`, `summary.md`, and `disagreements.json`. Do not copy predictions into production data, digests, handoffs, ranking, or track assignment.

## Stop Conditions

Stop if input evidence is missing, rules are not marked experimental, the output path is rejected, or production files change. Never schedule this command.
