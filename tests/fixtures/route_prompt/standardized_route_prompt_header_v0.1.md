# Standardized Route Prompt Header v0.1

Project root:
`D:\Code\CodexProjects\lattice-crypto-daily-digest`

Current active route:
Only public paper-radar route is active.

Operator:
`<Codex / DeepSeek-Claude / Kimi Code>`

Operator role:

- Codex: primary engineering/release-maintenance operator.
- DeepSeek-Claude: fallback runner/reviewer, no release ownership.
- Kimi Code: lightweight fallback runner/reviewer, no release ownership.

Forbidden:

- private paths;
- git add/commit/push/tag unless explicitly authorized in a separate phase;
- background automation;
- source/ranking/taxonomy changes unless the phase explicitly allows them;
- external LLM runtime;
- manual annotation workflow.

Required:

- pre-run checks;
- source-health caveats;
- durable artifact checks;
- final report;
- Codex review for code changes.
